define([
    'config',
    'jquery',
], function(config, $) {
    'use strict';
    function CCC() {
        
    }

    var prototype = {
        update: function() {
            
        }
    };

    $.extend(CCC.prototype, prototype);
    return CCC;
});